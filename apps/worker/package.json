{
  "name": "@corridor/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit",
    "vibe-poc": "bun run src/scripts/vibe-poc.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@corridor/shared": "workspace:*",
    "drizzle-orm": "^0.38.3",
    "postgres": "^3.4.5",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "^1.3.4",
    "typescript": "^5.7.2"
  }
}
